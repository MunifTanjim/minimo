comments:
  allowedFields: ["author", "email", "site", "content", "permalink"]
  branch: "master"
  commitMessage: "add [comment]: by {fields.author} <Staticman>\n\n{fields.permalink}#comment-{@id}"
  filename: "{@id}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp"
  moderation: false
  name: "Minimo"
  path: "data/comments/{options.postId}"
  requiredFields: ["author", "email", "author", "permalink"]
  transforms:
    email: md5
